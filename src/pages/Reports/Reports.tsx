// Pagina "KPI e report" — card numeriche riassuntive + grafici. traccar-web
// usa la libreria "recharts" per i grafici dei report: la adotteremo anche
// noi quando arriveremo a questo passo, per restare vicini alle scelte del
// progetto ufficiale (più facile confrontare/riusare codice in futuro).
import PlaceholderSection from '../../components/PlaceholderSection';

export default function Reports() {
  return (
    <PlaceholderSection
      title="KPI e report"
      description="Da costruire più avanti: card numeriche + grafici (eventi per ora, utilizzo flotta)."
    />
  );
}
