// Pagina "Manutenzione" — scadenze di manutenzione per veicolo. In Traccar
// esiste già il concetto di "Maintenance" legato ai dispositivi (vedi
// src/store/maintenances.js in traccar-web): potremo riusare la stessa idea.
import PlaceholderSection from '../../components/PlaceholderSection';

export default function Maintenance() {
  return (
    <PlaceholderSection
      title="Manutenzione"
      description="Da costruire più avanti: tabella scadenze con barre di avanzamento colorate."
    />
  );
}
