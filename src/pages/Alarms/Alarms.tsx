// Pagina "Allarmi e regole" — KPI di riepilogo + lista allarmi con azioni
// (presa in carico, escalation, chiusura). Corrisponde agli "Events" di
// Traccar (endpoint /api/events e/o notifiche via WebSocket).
import PlaceholderSection from '../../components/PlaceholderSection';

export default function Alarms() {
  return (
    <PlaceholderSection
      title="Allarmi e regole"
      description="Da costruire più avanti: card KPI + lista allarmi con badge di gravità e azioni (presa in carico, chiusura)."
    />
  );
}
