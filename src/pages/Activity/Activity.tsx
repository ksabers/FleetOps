// Pagina "Attività" — log cronologico delle azioni dell'operatore (es. prese
// in carico degli allarmi). Nella PoC era un semplice elenco che si popolava
// in tempo reale quando si agiva sugli allarmi.
import PlaceholderSection from '../../components/PlaceholderSection';

export default function Activity() {
  return (
    <PlaceholderSection
      title="Attività"
      description="Da costruire più avanti: cronologia delle azioni dell'operatore, condivisa con la pagina Allarmi."
    />
  );
}
